/**
 * modal.js
 *  Class that make and load a modal
 */
import { Setting } from "./settings";
export class Modal {
    /**
     * Set modal settings
     * @see Setting class
     * @param {Setting} settings 
     * @returns void
     */
    setSettings(settings) {}

    /**
     * Loads a modal
     * @param void
     * @returns Promise<any>
     */
    load() {
    }

    /**
     * Close the modal
     * @returns Promise<any>
     */
    close() {}
}